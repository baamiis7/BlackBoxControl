<UserControl x:Class="BlackBoxControl.SerialCommView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BlackBoxControl"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <TextBox Text="{Binding ReceivedData}" Width="400" Height="100"/>
            <Button Command="{Binding OpenCommand}" Content="Open Serial Port" Width="150" />
            <Button Command="{Binding CloseCommand}" Content="Close Serial Port" Width="150" />
            <Button Command="{Binding SendCommand}" Content="Send Data" Width="150" />
        </StackPanel>
    </Grid>
</UserControl>
