<UserControl x:Class="BlackBoxControl.CauseAndEffectsListForm"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource BackgroundBrush}"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>   <!-- sticky header + add button -->
            <RowDefinition Height="Auto"/>   <!-- stats strip -->
            <RowDefinition Height="Auto"/>   <!-- search + filter bar -->
            <RowDefinition Height="*"/>      <!-- C&E list — fills remaining space -->
        </Grid.RowDefinitions>

        <!-- ═══ HEADER ═══ -->
        <Border Grid.Row="0"
                Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,0,0,1"
                Padding="20,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="{DynamicResource PrimaryBrush}"
                        Width="36" Height="36" CornerRadius="4" Margin="0,0,12,0">
                    <TextBlock Text="⚡" FontSize="18" HorizontalAlignment="Center"
                               VerticalAlignment="Center" Foreground="White"/>
                </Border>

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="Cause and Effects Manager" FontSize="16" FontWeight="SemiBold"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBlock Text="Manage logical relationships between input and output devices"
                               FontSize="11" Foreground="{DynamicResource ForegroundDimBrush}"/>
                </StackPanel>

                <Button Grid.Column="2"
                        Content="+ Add New C&amp;E"
                        Style="{StaticResource PrimaryButton}"
                        MinWidth="140"
                        Command="{Binding AddNewCauseEffectCommand}"
                        VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- ═══ STATS STRIP ═══ -->
        <Grid Grid.Row="1" Margin="20,14,20,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Background="{DynamicResource SurfaceBrush}"
                    BorderBrush="{DynamicResource PrimaryBrush}"
                    BorderThickness="2" CornerRadius="6" Padding="14" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock Text="Total Entries" FontSize="11"
                               Foreground="{DynamicResource ForegroundDimBrush}" Margin="0,0,0,4"/>
                    <TextBlock Text="{Binding TotalEntries}" FontSize="28" FontWeight="Bold"
                               Foreground="{DynamicResource PrimaryBrush}"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1"
                    Background="{DynamicResource SurfaceBrush}"
                    BorderBrush="{DynamicResource SuccessBrush}"
                    BorderThickness="2" CornerRadius="6" Padding="14" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock Text="Active" FontSize="11"
                               Foreground="{DynamicResource ForegroundDimBrush}" Margin="0,0,0,4"/>
                    <TextBlock Text="{Binding ActiveEntries}" FontSize="28" FontWeight="Bold"
                               Foreground="{DynamicResource SuccessBrush}"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2"
                    Background="{DynamicResource SurfaceBrush}"
                    BorderBrush="{DynamicResource ForegroundDimBrush}"
                    BorderThickness="2" CornerRadius="6" Padding="14" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock Text="Inactive" FontSize="11"
                               Foreground="{DynamicResource ForegroundDimBrush}" Margin="0,0,0,4"/>
                    <TextBlock Text="{Binding InactiveEntries}" FontSize="28" FontWeight="Bold"
                               Foreground="{DynamicResource ForegroundDimBrush}"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="3"
                    Background="{DynamicResource SurfaceBrush}"
                    BorderBrush="{DynamicResource SecondaryBrush}"
                    BorderThickness="2" CornerRadius="6" Padding="14">
                <StackPanel>
                    <TextBlock Text="Most Used Gate" FontSize="11"
                               Foreground="{DynamicResource ForegroundDimBrush}" Margin="0,0,0,4"/>
                    <TextBlock Text="{Binding MostUsedGate}" FontSize="20" FontWeight="Bold"
                               Foreground="{DynamicResource SecondaryBrush}"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- ═══ SEARCH + FILTER ═══ -->
        <Border Grid.Row="2"
                Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1" CornerRadius="6"
                Margin="20,10,20,0" Padding="14,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Search box -->
                <Border Grid.Column="0"
                        Background="{DynamicResource BackgroundBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="1" CornerRadius="4" Padding="10"
                        Margin="0,0,10,0">
                    <Grid>
                        <TextBox Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 Background="Transparent" BorderThickness="0" Padding="0"/>
                        <TextBlock Text="Search cause and effects..."
                                   Foreground="{DynamicResource ForegroundDimBrush}"
                                   IsHitTestVisible="False" Margin="2,0,0,0">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding SearchText}" Value="">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>
                </Border>

                <ComboBox Grid.Column="1"
                          SelectedItem="{Binding FilterStatus, Mode=TwoWay}"
                          MinWidth="120" Margin="0,0,8,0">
                    <ComboBoxItem Content="All Status"/>
                    <ComboBoxItem Content="Active"/>
                    <ComboBoxItem Content="Inactive"/>
                </ComboBox>

                <ComboBox Grid.Column="2"
                          SelectedItem="{Binding FilterLogicGate, Mode=TwoWay}"
                          MinWidth="120">
                    <ComboBoxItem Content="All Gates"/>
                    <ComboBoxItem Content="OR"/>
                    <ComboBoxItem Content="AND"/>
                    <ComboBoxItem Content="XOR"/>
                </ComboBox>
            </Grid>
        </Border>

        <!-- ═══ C&E LIST (fills remaining height) ═══ -->
        <ScrollViewer Grid.Row="3"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      Margin="20,10,20,20">
            <StackPanel>

                <!-- Empty state -->
                <Border Padding="40"
                        Background="{DynamicResource SurfaceBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="1" CornerRadius="6">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding CauseEffects.Count}" Value="0">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock Text="⚡" FontSize="64" HorizontalAlignment="Center"
                                   Margin="0,0,0,10" Opacity="0.3"/>
                        <TextBlock Text="No Cause and Effect Entries Yet"
                                   FontSize="18" FontWeight="Medium"
                                   Foreground="{DynamicResource ForegroundBrush}"
                                   HorizontalAlignment="Center" Margin="0,0,0,5"/>
                        <TextBlock Text="Click '+ Add New C&amp;E' to create your first entry"
                                   FontSize="13" Foreground="{DynamicResource ForegroundDimBrush}"
                                   HorizontalAlignment="Center" Margin="0,0,0,20"/>
                        <Button Content="+ Create First Entry"
                                Style="{StaticResource PrimaryButton}"
                                Command="{Binding AddNewCauseEffectCommand}"/>
                    </StackPanel>
                </Border>

                <!-- C&E entry cards -->
                <ItemsControl ItemsSource="{Binding CauseEffects}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{DynamicResource SurfaceBrush}"
                                    BorderBrush="{DynamicResource BorderBrush}"
                                    BorderThickness="1" CornerRadius="6"
                                    Padding="15" Margin="0,0,0,8">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                                                <Setter Property="Background" Value="{DynamicResource HoverBrush}"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Icon + status badge -->
                                    <StackPanel Grid.Column="0" Orientation="Vertical"
                                                Margin="0,0,14,0" VerticalAlignment="Center">
                                        <TextBlock Text="⚡" FontSize="28"
                                                   HorizontalAlignment="Center"/>
                                        <Border CornerRadius="3" Padding="6,2" Margin="0,4,0,0">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Background" Value="{DynamicResource SuccessBrush}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding CauseEffect.IsEnabled}" Value="False">
                                                            <Setter Property="Background" Value="{DynamicResource ForegroundDimBrush}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <TextBlock Text="{Binding CauseEffect.Status}"
                                                       FontSize="10" Foreground="White"
                                                       FontWeight="Bold" HorizontalAlignment="Center"/>
                                        </Border>
                                    </StackPanel>

                                    <!-- C&E details -->
                                    <StackPanel Grid.Column="1">
                                        <TextBlock Text="{Binding CauseEffect.Name}"
                                                   FontSize="15" FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ForegroundBrush}"
                                                   Margin="0,0,0,8"/>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <Border Grid.Column="0"
                                                    Background="{DynamicResource BackgroundBrush}"
                                                    BorderBrush="{DynamicResource SecondaryBrush}"
                                                    BorderThickness="1" CornerRadius="4"
                                                    Padding="8,4" Margin="0,0,8,0">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="📥 " FontSize="11"/>
                                                    <TextBlock FontSize="11" Foreground="{DynamicResource ForegroundBrush}">
                                                        <Run Text="{Binding InputCount, Mode=OneWay}" FontWeight="Bold"/>
                                                        <Run Text=" input(s)"/>
                                                    </TextBlock>
                                                </StackPanel>
                                            </Border>

                                            <Border Grid.Column="1"
                                                    Background="{DynamicResource BackgroundBrush}"
                                                    BorderBrush="{DynamicResource WarningBrush}"
                                                    BorderThickness="1" CornerRadius="4"
                                                    Padding="8,4" Margin="0,0,8,0">
                                                <TextBlock FontSize="11" Foreground="{DynamicResource ForegroundBrush}">
                                                    <Run Text="⚙ " FontWeight="Bold"/>
                                                    <Run Text="{Binding CauseEffect.LogicGate}" FontWeight="Bold"/>
                                                </TextBlock>
                                            </Border>

                                            <Border Grid.Column="2"
                                                    Background="{DynamicResource BackgroundBrush}"
                                                    BorderBrush="{DynamicResource SuccessBrush}"
                                                    BorderThickness="1" CornerRadius="4"
                                                    Padding="8,4">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="📤 " FontSize="11"/>
                                                    <TextBlock FontSize="11" Foreground="{DynamicResource ForegroundBrush}">
                                                        <Run Text="{Binding OutputCount, Mode=OneWay}" FontWeight="Bold"/>
                                                        <Run Text=" output(s)"/>
                                                    </TextBlock>
                                                </StackPanel>
                                            </Border>
                                        </Grid>

                                        <TextBlock Margin="0,8,0,0" FontSize="11"
                                                   Foreground="{DynamicResource ForegroundDimBrush}"
                                                   TextWrapping="Wrap">
                                            <Run Text="When "/>
                                            <Run Text="{Binding InputCount, Mode=OneWay}" FontWeight="Bold"/>
                                            <Run Text=" input(s) trigger with "/>
                                            <Run Text="{Binding CauseEffect.LogicGate}" FontWeight="Bold"/>
                                            <Run Text=" logic → activate "/>
                                            <Run Text="{Binding OutputCount, Mode=OneWay}" FontWeight="Bold"/>
                                            <Run Text=" output(s)"/>
                                        </TextBlock>
                                    </StackPanel>

                                    <!-- Action buttons -->
                                    <StackPanel Grid.Column="2" Orientation="Vertical"
                                                VerticalAlignment="Center" Margin="10,0,0,0">
                                        <Button Content="Edit"
                                                Style="{StaticResource SecondaryButton}"
                                                MinWidth="70" Margin="0,0,0,4"
                                                Command="{Binding DataContext.EditCauseEffectCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding}"/>
                                        <Button Content="Delete"
                                                Style="{StaticResource DangerButton}"
                                                MinWidth="70" Margin="0,0,0,4"
                                                Command="{Binding DataContext.DeleteCauseEffectCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding}"/>
                                        <Button Content="Test"
                                                Style="{StaticResource SecondaryButton}"
                                                MinWidth="70"
                                                Command="{Binding DataContext.TestCauseEffectCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding}"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

            </StackPanel>
        </ScrollViewer>

    </Grid>
</UserControl>
