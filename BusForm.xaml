<UserControl x:Class="BlackBoxControl.BusForm"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource BackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Sticky Header -->
        <Border Grid.Row="0"
                Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,0,0,1"
                Padding="20,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="{DynamicResource SecondaryBrush}"
                        Width="36" Height="36" CornerRadius="4" Margin="0,0,12,0">
                    <TextBlock Text="🔌" FontSize="18" HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>
                </Border>

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="Bus Configuration" FontSize="16" FontWeight="SemiBold"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBlock Text="Configure communication bus settings and nodes"
                               FontSize="11" Foreground="{DynamicResource ForegroundDimBrush}"/>
                </StackPanel>

                <Border Grid.Column="2"
                        Background="{DynamicResource BackgroundBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="1" CornerRadius="4"
                        Padding="14,6" Margin="0,0,8,0">
                    <StackPanel>
                        <TextBlock Text="NODES" FontSize="10" FontWeight="SemiBold"
                                   Foreground="{DynamicResource ForegroundDimBrush}"
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding Bus.NumberOfNodes}" FontSize="20"
                                   FontWeight="Bold" Foreground="{DynamicResource PrimaryBrush}"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="3"
                        Background="{DynamicResource BackgroundBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="1" CornerRadius="4"
                        Padding="14,6">
                    <StackPanel>
                        <TextBlock Text="TYPE" FontSize="10" FontWeight="SemiBold"
                                   Foreground="{DynamicResource ForegroundDimBrush}"
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding Bus.BusType}" FontSize="14"
                                   FontWeight="Bold" Foreground="{DynamicResource SecondaryBrush}"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- Bus Config Fields -->
        <Grid Grid.Row="1" Margin="20,14,20,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <GroupBox Grid.Column="0" Header="Bus Name" Padding="12">
                <TextBox Text="{Binding Bus.BusName, UpdateSourceTrigger=PropertyChanged}"/>
            </GroupBox>

            <GroupBox Grid.Column="2" Header="Bus Type" Padding="12">
                <ComboBox SelectedItem="{Binding Bus.BusType}">
                    <ComboBoxItem Content="RS485"/>
                    <ComboBoxItem Content="RS232"/>
                    <ComboBoxItem Content="CAN"/>
                    <ComboBoxItem Content="Ethernet"/>
                </ComboBox>
            </GroupBox>
        </Grid>

        <!-- Nodes DataGrid — fills remaining height -->
        <Grid Grid.Row="2" Margin="20,14,20,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Nodes on this Bus"
                       FontSize="13" FontWeight="SemiBold"
                       Foreground="{DynamicResource ForegroundBrush}"
                       Margin="0,0,0,4"/>

            <TextBlock Grid.Row="1"
                       Text="Add nodes by dragging them from the device palette onto this bus in the Panel Explorer."
                       FontSize="11" Foreground="{DynamicResource ForegroundDimBrush}"
                       TextWrapping="Wrap" Margin="0,0,0,8"/>

            <DataGrid Grid.Row="2"
                      ItemsSource="{Binding Bus.Nodes}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      Background="{DynamicResource SurfaceBrush}"
                      RowBackground="{DynamicResource SurfaceBrush}"
                      AlternatingRowBackground="{DynamicResource BackgroundBrush}"
                      BorderBrush="{DynamicResource BorderBrush}"
                      BorderThickness="1"
                      Foreground="{DynamicResource ForegroundBrush}"
                      RowHeight="40">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
                        <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Padding" Value="10,8"/>
                        <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                        <Setter Property="BorderThickness" Value="0,0,1,1"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderBrush" Value="Transparent"/>
                        <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
                        <Setter Property="Padding" Value="10,5"/>
                    </Style>
                </DataGrid.CellStyle>
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="Image" Width="60">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Image Source="{Binding ImagePath}" Width="28" Height="28" Margin="4"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Type"     Binding="{Binding Name}"         Width="*"/>
                    <DataGridTextColumn Header="Address"  Binding="{Binding Address}"      Width="90"/>
                    <DataGridTextColumn Header="Location" Binding="{Binding LocationText}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <!-- Sticky Footer -->
        <Border Grid.Row="3"
                Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,1,0,0"
                Padding="20,12"
                Margin="0,12,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel"
                        Style="{StaticResource SecondaryButton}"
                        MinWidth="90" Margin="0,0,8,0"/>
                <Button Content="Save Changes"
                        Style="{StaticResource PrimaryButton}"
                        MinWidth="110" Margin="0,0,8,0"/>
                <Button Content="Add RS485 Node"
                        Style="{StaticResource SecondaryButton}"
                        MinWidth="130"/>
            </StackPanel>
        </Border>

    </Grid>
</UserControl>
